# REPO_INDEX.md

> **Purpose:** Single source of truth for repo state. PM reads this at every browser chat start to orient without guessing. CC maintains it at every close-out.

---

## Meta

- **Last updated:** 2026-04-24 (Session 50 — Experiment 2a Findings captured + fixture written. Two atomic commits: Commit 1 populates `/experiments/sinch/experiments-log.md §Experiment 2a Findings` with the 2026-04-24 run data (callback delay 1.769s, canonical payload shape with `type:"delivery_report_sms"` + `batch_id` correlation + `statuses[].code`/`status`/`recipients[]` per-entry shape, no signature header, Session 45 silent-drop hypothesis overturned), flips §Experiment 2a Status to `COMPLETE — captured 2026-04-24`, and writes `/experiments/sinch/fixtures/exp-02a-delivery-report.json` using the log step-5 nested schema (api_request / api_response / callback_request split, {SINCH_API_TOKEN} + {servicePlanId} placeholders); Commit 2 this close-out. No new D-numbers — hypothesis overturn is a finding about Sinch's behavior; enum-semantics decision already reserved for Phase 2 Session B kickoff per MASTER_PLAN §6.)
- **Decision count:** D-364 (next available: D-365). D-363 + D-364 added Session 48 per audit §E resolutions. Session 49 + Session 50 added no new D-numbers.
- **Master plan last updated:** 2026-04-23 (v1.1 — four edits: L3 version header v1.0 → v1.1; §5 Phase 1 item 2 replaced "Receive an inbound SMS (reply)" with 2a/2b-aware "Configure webhook receiver, capture callback payloads"; §6 full section replacement (Phase 2 renamed to "Sinch Outbound Delivery + Delivery-Report Callbacks", gains silent-drop-paragraph + enum-semantics-paragraph + 4 new What-gets-done bullets + routes MO to Phase 4 explicitly + rewrites demo moment/output for callback-driven status); §8 full section replacement (Phase 4 renamed to "Inbound Message Handling (MO Replies)", adds paragraph crediting receiver + signature verification to Phase 2, narrows What-gets-done to MO-specific work, narrows output to "MO handling works"). MESSAGE_PIPELINE_SPEC Session B spec catch-up deferred to kickoff per Session 46 scope boundary.)
- **Active CC session branch:** main
- **Unpushed local commits:** 2 unpushed at Session 50 close — `e811523` (Commit 1: Experiment 2a Findings + fixture, 2 files, +89 / -9) and this close-out (Commit 2: REPO_INDEX + CC_HANDOFF bumps). Both pending PM approval before push. **Pre-flight session-start git state (verified this session):** HEAD == `3d397a6` == `origin/main` at session start — all three Session 49 commits (`3d99a69` + `58ed165` + `3d397a6`) were pushed by Joel at Session 49 close via `git push origin main`, so this session began from a clean 0-unpushed state with the Session 49 `90a960c..3d397a6` range already on remote. Session-start reality check performed per the Session 47 fix-up lesson (verified via `git rev-list --left-right --count HEAD...origin/main` → `0 0`). Working tree clean except untracked `api/node_modules/` (intentional).

---

## Tier classification for browser chat uploads

**Tier 1 — Claude.ai project knowledge (permanent, rare updates):**
- `docs/VOICE_AND_PRODUCT_PRINCIPLES_v2.md`
- `docs/UNTITLED_UI_REFERENCE.md`

**Tier 2 — Upload at every browser chat start:**
- `REPO_INDEX.md` (this file)
- `MASTER_PLAN.md`
- `CC_HANDOFF.md`
- `PM_HANDOFF.md` (only if rotating chats)

**Tier 3 — Upload on demand when topic comes up:**
- Everything else below

---

## Canonical docs (repo root)

| File | Last touched | Purpose |
|------|-------------|---------|
| `README.md` | 2026-04-21 | Repo-root orientation — 38-line dev-doc pointing at canonical docs. Overwrote Untitled UI boilerplate Session 42. Pointers only, no facts restated. |
| `REPO_INDEX.md` | 2026-04-21 | This file |
| `MASTER_PLAN.md` | 2026-04-23 | Holistic 10-phase launch plan, North Star, customer values ranking, out-of-scope list, risks (v1.1). Session 46 scope amendment: Phase 2 gains delivery-report callback infrastructure (driven by Experiment 1 silent-drop finding); Phase 4 narrows to MO-specific inbound work; §5 Phase 1 item 2 rewritten for 2a/2b split. §4 "What gets done" past-tensed for Groups E (Session 40) + F (Session 41). |
| `PM_PROJECT_INSTRUCTIONS.md` | 2026-04-21 | Canonical PM instructions (synced to Claude.ai UI) |
| `CLAUDE.md` | 2026-04-21 | CC standing instructions (file-size discipline added Session 39 — keep under 200, target ~80). Session 42: removed retired `RELAYKIT_PRD_CONSOLIDATED.md` pointer from §Key docs (6 bullets → 5). |
| `DECISIONS.md` | 2026-04-21 | Active decisions D-84+, index of D-01–D-83 |
| `DECISIONS_ARCHIVE.md` | (stable) | Full text of D-01–D-83 |
| `PROTOTYPE_SPEC.md` | 2026-04-21 | Screen specs for `/prototype`. Session 40 Group E: example API key in Get-Started step flipped to `rk_test_` per D-349. |
| `WORKSPACE_DESIGN_SPEC.md` | 2026-04-21 | Post-signup workspace architecture. Session 40 Group E: example API key flipped to `rk_test_` per D-349. |
| `MESSAGE_PIPELINE_SPEC.md` | 2026-04-21 | `/api` pipeline — Session A + Consent API (complete), Session B (gated on Phase 1), Session C (not started) |
| `SDK_BUILD_PLAN.md` | 2026-04-21 | `/sdk` retrospective (v0.1.0 shipped) + Phase 8 delivery spec (README, AGENTS.md, npm publish). Rewritten Session 39 (Phase 0 Group D); Session 40 Group E flipped §status-table and §7 follow-up from Pending to Complete. |
| `SRC_SUNSET.md` | 2026-04-21 | `/src` capability-to-phase map driving Phase 2–5 rebuilds per D-358. Active reference; retires when Phase 5 closes. Added Session 41 (Phase 0 Group F). Session 42: companion pointer L5 path updated to `docs/archive/CURRENT_STATE_AUDIT.md §2.1`. |
| `CC_HANDOFF.md` | 2026-04-21 | Previous CC session state |
| `BACKLOG.md` | 2026-04-10 | Parked ideas, never build without promotion |
| `audits/DECISIONS_AUDIT_2026-04-24.md` | 2026-04-24 | Read-only DECISIONS audit (Session 47): D-01–D-362 against MASTER_PLAN v1.1 + VOICE v2.0 §6. **Resolved Session 48** — 16 findings executed as inline annotations + 2 new D-numbers (D-363, D-364); 1 no-action (C-4); 1 unsupported-claim appendix item (D-241→D-211) deferred to PM. First entry under `audits/` directory; convention: dated audit reports, never overwritten — this report remains as historical record. |

---

## Canonical docs (`/docs`)

| File | Last touched | Purpose |
|------|-------------|---------|
| `PRICING_MODEL.md` | 2026-04-21 (v6.0) | Tier definitions, costs, pricing logic. Session 40 Group E: `rk_sandbox_`→`rk_test_` prefix flip in the sandbox-key-included bullet. No version bump — D-349 application, not a pricing-logic change. |
| `PRD_SETTINGS_v2_3.md` | 2026-04-15 | Settings page spec |
| `VOICE_AND_PRODUCT_PRINCIPLES_v2.md` | (stable) | Copy rules (Tier 1 in project knowledge) |
| `UNTITLED_UI_REFERENCE.md` | (stable) | Design system reference (Tier 1 in project knowledge) |
| `STARTER_KIT_PROGRAM.md` | 2026-04-17 | Starter kit strategy — superseded by MASTER_PLAN Phase 9 (third-party integration over building our own) |
| `AI_INTEGRATION_RESEARCH.md` | 2026-04-15 | AGENTS.md, Resend analog, tool prompts |

---

## Subdirectories

### `/docs/archive` (superseded, not operational)

Historical PRDs, vision docs, old strategy. CC does not read these unless Joel directs to a specific one.

Notable contents: `BUILD_SPEC_VALIDATION_LOG.md` (25 experiment rounds), Twilio-era PRDs (01–09), early vision docs, superseded design references.

Deprecation headers added Session 38: `PRD_04_TWILIO_SUBMISSION.md` (cites D-358), `PRICING_MODEL.md` (superseded by v6.0). `VISION_IMPLEMENTATION_MEMO.md` deleted Session 38 per its own "consume and discard" header — D-104's reference to it is now accompanied by a supersession note pointing to D-358.

### `/docs/plans`

Old UX prototype plans. Currently contains `2026-03-08-ux-prototype.md`. Superseded by current prototype + PROTOTYPE_SPEC; keeping for history only.

### `/docs/superpowers`

CC's internal artifacts from the `superpowers` skill (claude-plugins-official). CC-managed. PM does not touch. Current design docs under `/specs`: `2026-04-17-message-editor-tiptap-design.md` (Tiptap editor architecture, D-354).

### `/prototype`

Active prototype UI (port 3001). Source of truth for UI.

Notable subtrees added Session 35:
- `lib/editor/` — Tiptap editor scaffolding (`message-editor.tsx`, `variable-node.ts`, `variable-node-view.tsx`, `template-serde.ts`)
- `lib/variable-token.ts` — shared color-only class for variable rendering across editor + previews
- `lib/variable-scope.ts` — per-message variable scope helper (D-353)

New files Session 36 (custom message CRUD — D-351 revised):
- `lib/slug.ts` — kebab-case slug generator with numeric-suffix collision handling
- `components/catalog/custom-message-card.tsx` — sibling of `CatalogCard`, authoring + monitor UI for custom messages
- `components/catalog/message-action-modal.tsx` — parameterized modal reused by Archive + Delete permanently
- `components/edit-business-details-modal.tsx` — prototype-only dev modal for swapping `appName` / `serviceType` from the state-switcher dropdown

Removed Session 35 (dead code):
- `components/plan-builder/message-card.tsx`
- `components/plan-builder/message-tier.tsx`

### `/src`

**Sunset per D-358 (2026-04-20).** Legacy Twilio-era production codebase, frozen — no modifications. Capabilities being rebuilt on `/api` + Sinch across MASTER_PLAN Phases 2–5. Excluded from the Phase 0 Group E `rk_sandbox_`→`rk_test_` sweep by PM direction (consistent with freeze). Sunset map document (`SRC_SUNSET.md`) deferred from Session 38 to Group F in a later session.

### `/sdk`

RelayKit npm package. v0.1.0 packed as `.tgz` at `/sdk/relaykit-0.1.0.tgz`, not yet published to npm. 8 namespaces, 30 methods (class-based `new RelayKit()` init, zero-config `process.env`), dual ESM/CJS via tsup, 235-line test suite green. Canonical `SendResult` shape pinned by D-362. SDK_BUILD_PLAN.md rewritten Session 39 to match shipped reality. Missing: README, AGENTS.md, published release — scoped to MASTER_PLAN Phase 8.

### `/api`

Message delivery backend (Hono + Sinch-shaped). Session A shipped. Consent API shipped and now documented as COMPLETE in MESSAGE_PIPELINE_SPEC (promoted from "Future additions" Session 38). Session B status GATED — start depends on Phase 1 Sinch experiments producing recorded request/response shapes. `/api/supabase/migrations/005_messages_table.sql` exists but not yet applied. A `consent-check` pipeline step is planned but not yet wired — belongs with Session B (send wiring) or a dedicated Session D; not currently in any MASTER_PLAN phase's scope.

### `/supabase`

Root-level migrations directory — `/src`-era, Twilio-shaped. **Slated for archive in Phase 3** (database reconciliation). Contains destructive audit migration flagged in audit §5.13. `/api/supabase/` will become the single source of truth.

### `/audits`

Dated read-only audit reports. Created Session 47 with `DECISIONS_AUDIT_2026-04-24.md` (pre-Phase-2 DECISIONS ledger audit, BACKLOG-promoted). Convention: each report is dated `YYYY-MM-DD` in the filename, never overwritten — superseded audits stay for historical comparison. Reports are findings-only by design; cleanup actions land in DECISIONS.md / DECISIONS_ARCHIVE.md only after explicit PM triage and never as part of the audit commit itself.

### `/experiments/sinch/`

Phase 1 Sinch proving-ground experiments. `experiments-log.md` with captured response shapes, measured timings, webhook payloads. Seeded Session 43 with Experiment 1 procedure; Session 44 captured real findings (Experiment 1 success-path fixture + Experiment 1b delivery-report-rejection fixture), filled in Findings + Implications. Session 45 replaced the original Experiment 2 stub with a 2a/2b split. Session 49 scaffolded the Cloudflare Worker at `/experiments/sinch/webhook-receiver/`; Joel deployed it + configured the Sinch Service Plan callback URL between Session 49 and 50. **2a (delivery-report callback shape) — COMPLETE (Session 50, 2026-04-24):** Findings populated, canonical shape captured in `fixtures/exp-02a-delivery-report.json` (delivery-report payload with `type:"delivery_report_sms"` discriminator, `batch_id` ULID correlation, `statuses[]` array with per-entry `{ code, count, recipients, status }`, callback delay 1.769s from send to arrival). Key Session 45 hypothesis overturned: Sinch DOES notify us of carrier failures via callback, within ~2 seconds — failure-detection for Phase 2 is callback-primary with timeout fallback as safety net. No signature header on the callback — Phase 2 signature verification design must use IP allowlist / mTLS / secret path segment / unenabled Sinch feature instead of standard HMAC. **2b (inbound MO shape) — still BLOCKED on Experiments 3 + 4** (unregistered 10DLC can't receive messages). Code in `webhook-receiver/` retained per the One Source Rule rather than thrown away — becomes a real Phase 2/4 prototype (Phase 2 ports the Worker into `/api` as a real route per MASTER_PLAN §6). `fixtures/` contains 3 captured JSON fixtures (`exp-01-outbound.json`, `exp-01b-delivery-report-rejected.json`, `exp-02a-delivery-report.json`). All fixtures use `{servicePlanId}` and `{SINCH_API_TOKEN}` placeholders so no live credentials enter git. Other carriers (if ever evaluated) would get sibling directories — structure is `/experiments/{carrier}/`.

---

## Build spec status

| Spec | Status | Implementation |
|------|--------|----------------|
| MESSAGE_PIPELINE_SPEC Session A | COMPLETE | `/api/src/pipeline/` — normalize, interpolate, send stub, log stub. |
| MESSAGE_PIPELINE_SPEC Consent API | COMPLETE | Documented as shipped in MESSAGE_PIPELINE_SPEC Session 38. Endpoints `record / check / revoke` live in `/api/src/routes/consent.ts`; tests green. `consent-check` pipeline step planned but not yet wired. |
| MESSAGE_PIPELINE_SPEC Session B | GATED | Sinch account unblocked 2026-04-20. Start requires Phase 1 Sinch experiment findings (MASTER_PLAN §5). Session 50 captured Experiment 2a: delivery-report callback shape recorded (`fixtures/exp-02a-delivery-report.json`), callback delay measured at ~1.77s, and the Session 45 silent-drop hypothesis overturned — Sinch DOES deliver carrier-failure signals via callback within ~2s. Callback-primary + timeout-fallback is the Phase 2 failure-detection pattern. Send-path + callback-path contracts now have real shapes (not hypotheses). Remaining gates: Experiments 3 + 4 (registration) + 5 (STOP/START/HELP) — procedures not yet drafted. Session B unblocks for kickoff on the delivery-path axis; the MESSAGE_PIPELINE_SPEC Session B spec catch-up + `messages.status` enum-semantics D-number still land at kickoff per Session 46 design. |
| MESSAGE_PIPELINE_SPEC Session C | DEFERRED | Quiet hours / queueing deferred to post-launch unless real customer need emerges. |
| SDK_BUILD_PLAN | CURRENT (delivery pending) | Rewritten Session 39 as retrospective (v0.1.0 shipped: 8 namespaces, 30 methods, tsup dual-format, test suite green, canonical `SendResult` shape pinned by D-362) + Phase 8 delivery spec (npm publish, 13-section README, AGENTS.md template, integration prompt). |
| Workspace message row evolution | IN PROGRESS | Shared grid + /account shipped Session 32–33; Tiptap editor + atomic variable tokens shipped Session 35 (D-350, D-353, D-354). Custom message CRUD shipped Session 36 (D-351 revised) — `+ Add`, edit/slug/save, Archive + Delete modals, archived disclosure, monitor mode on saved customs, pre-populated compliant body with business-name chip (D-356), lock-while-authoring (D-357). Next: D-352 (content-based marketing classification) and D-355 (variable grammar rendering). |
| Vertical hydration (wizard → workspace) | NOT STARTED | Audit §3 documents broken handoff. Covered by MASTER_PLAN Phase 6. Only Appointments reachable end-to-end; 7 other verticals are fully populated data but unreachable. |

---

## Active plan pointer

**Active master plan phase:** Phase 1 (Sinch proving-ground experiments) — **ACTIVE**, Joel-driven (PM writes procedures, Joel runs experiments, CC on standby for scaffolds). Phase 0 closed at Session 41 with Group F completion. Session 42 (docs hygiene) and Session 43 (MASTER_PLAN cross-ref cleanup + `/experiments/sinch/` scaffold) run between Phase 0 close and the first experiment.

**Phase 1 scope (per MASTER_PLAN §5):** five experiments characterizing Sinch before Phase 2 Session B builds production code against it.

1. **Experiment 1 — provision number + send one SMS.** Procedure seeded in `/experiments/sinch/experiments-log.md` Session 43. Status: not yet run. Produces the outbound request/response fixture Phase 2 Session B will consume.
2. **Experiment 2a — delivery-report callback shape.** **COMPLETE (Session 50, 2026-04-24).** Captured in `fixtures/exp-02a-delivery-report.json`. Callback delay ~1.77s; canonical payload shape with `type:"delivery_report_sms"` discriminator + `batch_id` correlation + `statuses[]` array. Session 45 silent-drop hypothesis overturned. No signature header on callback — flagged for Phase 2 Session B kickoff discussion. **Experiment 2b — inbound MO message shape.** Still BLOCKED on Experiments 3 + 4 (unregistered 10DLC can't receive replies). Will produce the fixture Phase 4 consumes.
3. **Experiment 3 — brand registration submission + timing.** Measures the fast-registration promise. Produces the registration submission response shape.
4. **Experiment 4 — campaign registration submission + timing.** Completes the registration flow end-to-end.
5. **Experiment 5 — STOP/START/HELP handling.** Determines whether Sinch auto-handles opt-outs at the carrier layer (affecting Phase 4's scope).

Code in `/experiments/sinch/` is throwaway. What ports into `/api` is *recorded shapes + measured behavior*, not the experiment code itself.

**Phase 0 progress (CLOSED — Session 41):** per Session 38 plan `phase-0-doc-reconciliation-work-temporal-brook.md` + Session 39 Group D refinement `group-d-sdk-build-plan-md-modular-hanrahan.md`.
- **Groups A/B/C — COMPLETE (Session 38).** See 2026-04-21 Session 38 change log entry.
- **Group A #2 residual — COMPLETE (Session 39, `35a279a`).** `-- DEV ONLY — DO NOT RUN IN PROD` banner landed on `/supabase/migrations/20260307200000_audit_fixes.sql`.
- **Group D — COMPLETE (Session 39, `a202b7d` + D-362 `165f30a`).** SDK_BUILD_PLAN rewritten as v0.1.0 retrospective plus Phase 8 delivery spec. D-362 recorded pinning the shipped `SendResult` shape as canonical (purely additive, no supersession of D-277). R1/R2/R3 folded into prose (no `⚠ CONFLICT` markers); R4 voice/pricing re-read of §5 integration prompt found clean, noted inline as 2026-04-21 voice check.
- **Group E — COMPLETE (Session 40, 2026-04-21):** `rk_sandbox_`→`rk_test_` sweep applied across `/api` code + tests + user-facing copy + reference/spec docs in three atomic commits (E1/E2/E3). `tsc --noEmit` clean on `/api`, `/sdk`, `/prototype`; `/api` vitest 98/98 green. `/src` and archive docs intentionally excluded per D-358 freeze and historical-preservation rule.
- **Group F — COMPLETE (Session 41, 2026-04-21):** `SRC_SUNSET.md` created at repo root — `/src` capability-to-phase map driving Phase 2–5 rebuilds per D-358. Covers all 23 `/src/app/api/*` routes plus dashboard/auth/encryption utilities: 2 Phase 2 rows (outbound pipeline, delivery status webhook), 1 Phase 3 row (root `/supabase/` archival), 4 Phase 4 rows (inbound webhook, STOP/START/HELP, opt-out API, developer webhook CRUD), 8 Phase 5 rows (registration pipeline, account mgmt, Stripe webhooks, Stripe checkout, auth system, dashboard UI, compliance monitoring, compliance site preview), 1 Phase 0 completed row (sandbox key mgmt — rebuild shipped in Session A pre-Phase 0, prefix flip in Group E), plus 8 excluded/not-rebuilt rows (BYO waitlist, use-case, blueprint endpoints, dev/intake weirdness, unreachable auth page, unused hooks, Twilio lib, subaccount encryption). Five Open-F-N mappings surfaced for target-phase resolution (delivery-status scope, webhook CRUD placement, Stripe checkout confirmation, auth rebuild confirmation, compliance-monitoring scope clarification) — deliberately not resolved in Phase 0. MASTER_PLAN §4 "What gets done" list gained a past-tense Group F bullet at L101. Closes Phase 0.

---

## Decision count verification

Verify `D-362` against DECISIONS.md at chat start. If drifted, update this file.

---

## Sync obligations

- **CC at every close-out:** bump `Last updated`, `Decision count`, add/remove files, update build spec status, update `Master plan last updated` if MASTER_PLAN.md was edited, verify `PM instructions synced` flag if Joel confirmed a paste.
- **PM when proposing MASTER_PLAN changes:** write to `MASTER_PLAN.md`, bump `Master plan last updated` date, note version bump in MASTER_PLAN's own header if substantive. No Claude.ai UI sync required — master plan lives only in repo.

---

## Change log

- **2026-04-17:** Initial REPO_INDEX created. Introduces tiered file orchestration, sync tracking between repo and Claude.ai UI project instructions, canonical file listings at root and `/docs`.
- **2026-04-17 (Session 34):** Added `MESSAGE_PIPELINE_SPEC.md` and `SDK_BUILD_PLAN.md` at repo root; expanded `docs/STARTER_KIT_PROGRAM.md` with sections 12–19; recorded D-349 through D-353; build spec status for SDK flipped to "Spec ready".
- **2026-04-18 (Session 35):** Tiptap v3 wired into the workspace message editor with atomic `VariableNode` atoms (D-350) and a `+ Variable` insert affordance scoped per-message (D-353). Library choice recorded as D-354. Added `prototype/lib/editor/` + `prototype/lib/variable-{token,scope}.ts`; deleted unused `plan-builder/message-{card,tier}.tsx`. Design doc at `docs/superpowers/specs/2026-04-17-message-editor-tiptap-design.md`. PROTOTYPE_SPEC Edit-State section rewritten.
- **2026-04-18 (Session 35 close-out):** Error-state bug resolution. Root cause was Tiptap's `Editor.setEditable` emitting the `update` event unconditionally with stale doc content. Two prior fix attempts (`cf09e61` `hasUserEdited` gate; `55a87e5` explicit-call compliance refactor) treated symptoms on the CatalogCard side and did not resolve. Final fix `858866d`: `editor.setEditable(!disabled, false)` in `message-editor.tsx`. The `55a87e5` architecture is retained as still correct. Design doc §15 added with the Tiptap usage rule + call-site audit. All 12 session commits pushed to `origin/main`.
- 2026-04-18: D-351 revised — generic SDK method with slug supersedes prior manual-send-only formulation.
- 2026-04-18 (Session 36 start): D-355 recorded — variable grammar: canonical base form for common nouns + context-aware rendering; proper nouns stored and rendered as-is.
- **2026-04-19 (Session 36):** Custom message CRUD shipped end-to-end. `+ Add` inserts a pre-populated compliant row at the top of the stack with business-name chip + placeholder + opt-out phrase (D-356). Saved rows match built-in visual language — name + slug inline, kebab + activity + pencil chrome, monitor expansion with Test send / Ask Claude / Close. Archive + Delete permanently flows via shared `MessageActionModal`; Archived (N) disclosure at the bottom; archived rows render full-contrast (PM pushback on muted styling) and are read-only. Compliance gained a second rule for business-name with its own Fix button (D-356). Lock-while-authoring supersedes auto-discard: sibling affordances disabled with "Save or cancel the current message first." tooltip while a never-saved custom is open (D-357). User-facing "Testers" → "Preview list" copy rename (code identifiers untouched). Dashboard status indicator yellow-state "Test mode" → "Test messages only". Monitor-footer "Quick send" → "Test send" with source-of-message tooltip. Added Reset action to onboarding dropdown + Edit business details dev modal on the state-switcher dropdown. Tooltip token regression fix — Session 35's semantic-token migration referenced `--color-bg-primary-solid` without defining it; added both `bg-primary-solid` and `bg-secondary-solid` to `globals.css` @theme.
- 2026-04-19: D-356 + D-357 recorded. PM_PROJECT_INSTRUCTIONS updated with response-brevity and step-by-step instruction guidelines; paste confirmed, `pm_instructions_synced` stays `true`.
- **2026-04-20 (Session 37 — audit-only, no build work):** `CURRENT_STATE_AUDIT.md` added at repo root. Documents per-directory inventory (`/src`, `/api`, `/sdk`, `/prototype`, `/supabase`, `/api/supabase`, `/docs`, `/docs/archive`), spec-to-reality gaps for MESSAGE_PIPELINE_SPEC / SDK_BUILD_PLAN / PROTOTYPE_SPEC / WORKSPACE_DESIGN_SPEC / RELAYKIT_PRD_CONSOLIDATED / PRICING_MODEL, end-to-end trace for the Verification-codes vertical (wizard→workspace handoff is broken; data is complete but unreachable), 26-item weirdness log, Sinch readiness review, and recommendations list. No decisions bumped, no sync flags flipped. `tsc --noEmit` clean on `/api`, `/sdk`, `/prototype` at audit time; `/api` tests 98/98 green. See audit §5 for drift summary (`rk_sandbox_`/`rk_test_` sweep deferred, two backends + two supabase dirs, inbound split-brain, SDK_BUILD_PLAN stale, CLAUDE.md key-name drift).
- **2026-04-20 (same chat, post-audit):** Sinch account unblocked — $100 charged, phone number can be provisioned. Session B status flipped BLOCKED → UNBLOCKED (pending Phase 1 experiments). Scoping conversations held for OTP/Verification, review requests, two-way messaging; verification card observed in prototype triggered reconsideration and discovery that Verification is a fully-populated vertical with broken wizard→workspace wiring (per audit §3). `MASTER_PLAN.md` v1.0 created at repo root — holistic 10-phase launch plan with North Star, ranked customer values (fast registration added as top value — the Sinch differentiator), working principles, explicit out-of-scope list, risks. Added as Tier 2 upload. New `Master plan last updated` field added to Meta. `/experiments/` directory planned for Phase 1. Two D-numbers flagged as needed but not yet recorded (D-358 `/src` sunset, D-359 MASTER_PLAN adoption) — to be recorded in next CC session once PM↔Joel decisions are finalized.
- **2026-04-20 (Session 37 close-out):** D-358 (`/src` sunset — rebuild registration/inbound/dashboard/billing on `/api` + Sinch, no Twilio preservation), D-359 (MASTER_PLAN.md v1.0 adopted as canonical), D-360 (OTP/Verification as both cross-vertical auth primitive AND dedicated vertical), D-361 (review-request templates ship at launch as vertical-internal additions using developer-supplied review URLs) all recorded. Active plan pointer updated to reflect Phase 0 active / Phase 1 unblocked in parallel. `PM_PROJECT_INSTRUCTIONS.md` substantially rewritten (273 lines of diff, +/- across the file). `tsc --noEmit` clean on `/api`, `/sdk`, `/prototype` at close-out. No code changes — docs-only session following the audit.
- **2026-04-21:** `PM_PROJECT_INSTRUCTIONS.md` — File Requests + CC Mode Signaling sections added (committed standalone as PM-side work, not Phase 0 doc reconciliation).
- **2026-04-21 (Session 38 — Phase 0 Groups A–C, partial):** Phase 0 doc-reconciliation plan drafted at `~/.claude/plans/phase-0-doc-reconciliation-work-temporal-brook.md` (10 groups scoped A–G, dependencies, session-rotation points, effort estimates). Group A PM clarifications resolved — D-03 "magic-link" retained as shorthand for Supabase's passwordless-email family (no new decision); migration banner approved but not yet executed (flagged as Group A #2 residual); `/src` excluded from Group E sweep per freeze; VISION_IMPLEMENTATION_MEMO approved for deletion. Group B shipped five spot fixes across five files — CLAUDE.md wizard key (`relaykit_intake`→`relaykit_wizard`) and auth-mechanism clarification, PRD decision count `329+`→`361+`, deprecation headers on `docs/archive/PRD_04_TWILIO_SUBMISSION.md` and `docs/archive/PRICING_MODEL.md`, deletion of `docs/archive/VISION_IMPLEMENTATION_MEMO.md`. Two follow-up commits after Group B: PRD endpoint list expanded from five to six shipped `/api` endpoints (adds `POST /v1/signup/sandbox`), and D-104 supersession note appended citing D-358. Group C shipped Consent API promotion to `[COMPLETE]` section in MESSAGE_PIPELINE_SPEC, Session B flipped `BLOCKED`→`GATED` on Phase 1 experiments, Future/Consent subsection removed. No new D-numbers (all work was documentation catching up to reality). Flagged for tracking: `consent-check` pipeline step belongs in Phase 2 but isn't scoped there; migration runtime-application status is a Phase 3 question; Group A #2 migration banner is a residual. PM-side commit: `PM_PROJECT_INSTRUCTIONS.md` gains File Requests + CC Mode Signaling sections. `tsc --noEmit` + `eslint` clean on `/api` and `/sdk`; `tsc --noEmit` clean on `/prototype`. Five Phase 0 commits + one PM instructions commit (`66f2872`, `cb25bd1`, `54918a6`, `0e4f56e`, `939d83e`). Groups D (SDK_BUILD_PLAN rewrite), E (`rk_sandbox_` sweep), F (`SRC_SUNSET.md`) pending for subsequent CC sessions.
- **2026-04-21 (Session 39 — Phase 0 Group A #2 + Group D, plus bookkeeping):** Opened with bookkeeping scar repair — CC_HANDOFF header and gotcha 7, plus REPO_INDEX meta line, corrected from stale "NOT pushed" language after confirming `HEAD == origin/main` from Session 38 (`079c77d`). Group A #2 residual shipped standalone — `-- DEV ONLY — DO NOT RUN IN PROD` banner on `/supabase/migrations/20260307200000_audit_fixes.sql` (`35a279a`). Group D (SDK_BUILD_PLAN rewrite) executed in plan mode — plan at `~/.claude/plans/group-d-sdk-build-plan-md-modular-hanrahan.md` approved with resolutions to four reconciliation points (R1 SendResult shape, R2 init pattern, R3 missing D-numbers, R4 integration-prompt drift). D-362 recorded pinning the shipped `SendResult` shape `{ id, status, reason? }` as canonical — standalone commit before the rewrite, purely additive after determining D-277 describes semantics not shape (`165f30a`; originally `ecc0590`, amended to fix §7→§6 cross-reference in the Affects line, re-parented as `165f30a`). Full SDK_BUILD_PLAN rewrite (`a202b7d`; originally `b6f886f`, re-parented after the D-362 amend) replaces the forward-looking "NOT STARTED" narrative with a v0.1.0 retrospective (shipped surface, tsup build, tests, shipped-behaviors reference) plus a Phase 8 delivery section (npm publish, 13-section README spec, AGENTS.md template, integration prompt). Every example updated to use the shipped `new RelayKit()` pattern; every SendResult reference updated to the canonical shape. §6 Guided vs Quick Start UI spec deleted entirely (not relocated). §1 Steps 1–5 folded into retrospective; §1 Step 6 moved to Phase 8 section. R3 added D-306, D-307, D-308, D-351, D-362 to the decisions list (plus D-360, D-361 cited in the README §6 surface description — slightly broader than plan's expected 15; PM may trim). R4 voice/pricing re-read of §5 integration prompt found clean (no pricing claims, no prohibited compliance-guarantee language, clean imperative tone); noted inline as a 2026-04-21 voice check. File landed at 461 lines vs plan's 280–320 target — dense, not bloated (§2 retrospective + retained 13-section README spec); PM may direct trim pass. CLAUDE.md size-discipline section added at top — keep under 200 lines, target ~80 (`bf4be60`; CLAUDE.md now 111 lines). `tsc --noEmit` clean on `/api`, `/sdk`, `/prototype` at close-out; no code touched this session. Session 39 commits: `079c77d`, `35a279a`, `165f30a`, `a202b7d`, `bf4be60`, plus this close-out commit — all pushed to `origin/main` on top of Session 38's tip (`838525e`). Active plan pointer updated to reflect Groups A #2 + D complete; Groups E (`rk_sandbox_` sweep) and F (`SRC_SUNSET.md`) remain pending.
- **2026-04-21 (Session 40 — Phase 0 Group E):** `rk_sandbox_`→`rk_test_` sweep executed in plan mode; plan at `~/.claude/plans/group-e-rk-sandbox-rk-test-sharded-widget.md` with six open questions resolved by PM before execution (D-285 appended supersession note; archive docs skipped; superpowers plan artifact treated as archive; three atomic commits E1/E2/E3; MASTER_PLAN L366 deleted outright; Group F standalone session next). Re-verified inventory: 40 matches across repo excluding build-artifact dirs; 10 in top-level `/src` (frozen per D-358) excluded; 30 in-scope; +1 drift from plan's 29 estimate (the superpowers plan artifact, treated as archive). **E1** (`136f2b5`, 9 files, 29/-29): prefix generator at `api/src/routes/signup.ts:17` flipped (real behavior change — `/v1/signup/sandbox` now mints `rk_test_*`); `api/src/__tests__/signup.test.ts` regex assertions flipped in lockstep; 6 other /api test files + 1 migration comment. **E2** (`7dcb02f`, 4 files, 4/-4): `prototype/components/setup-instructions.tsx`, `prototype/app/apps/[appId]/get-started/page.tsx`, `docs/PRICING_MODEL.md` (prefix only — `Sandbox API key` noun phrase preserved), `WORKSPACE_DESIGN_SPEC.md` example key. **E3** (`b093af0`, 6 files, 8/-6): D-285 gained `_⚠ Partial supersession: user-facing prefix is now rk_test_ per D-349._` appended between body and `_Affects:_` footer; MASTER_PLAN.md §4 L100 past-tensed with completion annotation; MASTER_PLAN.md §14 former L366 Phase 10 bullet deleted; SDK_BUILD_PLAN.md §status-table L27 and §7 L457 flipped to past-tense/Complete; PROTOTYPE_SPEC.md §571 example key flipped; CURRENT_STATE_AUDIT.md §5.7 gained a `**Resolved:**` banner (body preserved as frozen snapshot); REPO_INDEX.md L150 Group E bullet flipped PENDING→COMPLETE. **Historical text preserved intentionally:** D-349 body at DECISIONS.md L1222/L1224 (the decision text *explains* the rename), CURRENT_STATE_AUDIT §1/§2/§4/§7 snapshots, REPO_INDEX L108 + L180/L184/L185 session-log entries, all `/src/**` and `docs/archive/**` + `docs/superpowers/plans/**` references. Verification grep returned zero unexpected drift. Quality gates: `tsc --noEmit` clean on `/api`, `/sdk`, `/prototype` (re-ran after E1 and pre-close); `/api` vitest 98/98 green (matches Session 37 baseline); `eslint` clean on all three packages at close-out. All three Group E commits pushed to `origin/main` mid-session after PM approval (`d6a2c7a..b093af0`). CC_HANDOFF.md and this REPO_INDEX update land as a single close-out commit, push pending PM approval. Group F (`SRC_SUNSET.md`) deferred to a standalone next session per PM resolution #6. No new D-numbers.
- **2026-04-21 (Session 42 — docs hygiene pass, between Phase 0 close and Phase 1 start):** Two canonical-docs retirements and a new root README.md (commit `b7e93ad`). `docs/RELAYKIT_PRD_CONSOLIDATED.md` archived to `docs/archive/RELAYKIT_PRD_CONSOLIDATED.md` (Phase 0 coherence audit found 5 factual drift points — Twilio "remap" language post-D-358, SDK v0.1.0 described not-yet-built, Consent API described not-yet-built, missing D-358/359/360/361 citations, pricing "TBD" despite PRICING_MODEL v6.0 settled; root cause was the same facts living in PRD_CONSOLIDATED + MASTER_PLAN + PRICING_MODEL + SDK_BUILD_PLAN and drifting independently; retirement eliminates the duplication pattern rather than fixing the 5 drifts in place). `CURRENT_STATE_AUDIT.md` archived from repo root to `docs/archive/CURRENT_STATE_AUDIT.md` (the Session 37 snapshot's job was to identify Phase 0 gaps; Phase 0 closed Session 41; actionable `/src` findings carried forward by `SRC_SUNSET.md`). Both archived files prepended with a two-line blockquote deprecation header citing retirement date + reason + "See instead" pointers to canonical sources. Body content preserved verbatim in both — `git mv` with 99%/98% rename similarity, history follows. Root `README.md` overwritten — was 57 lines of Untitled UI starter boilerplate with no RelayKit-specific content; now a 38-line dev-doc orienting cold readers in under 5 minutes (title + one-paragraph "what RelayKit is" quoting MASTER_PLAN §0 + 5-item reading list + 4-item directory map + current-phase pointer + prototype-only Local development block + one-line design-system reference). Cross-references updated: `CLAUDE.md` §Key docs lost the `RELAYKIT_PRD_CONSOLIDATED.md` bullet (6 → 5); `REPO_INDEX.md` canonical-docs tables lost both retired rows, gained a README.md row at the top of the root table; `SRC_SUNSET.md:5` companion pointer path updated `CURRENT_STATE_AUDIT.md` → `docs/archive/CURRENT_STATE_AUDIT.md`. Rule established: every fact lives in exactly one canonical doc; others reference, never restate. **Cross-refs flagged but intentionally not touched this session** (for PM resolution at next session boundary): `MASTER_PLAN.md:33` (narrative "as of the CURRENT_STATE_AUDIT done this session") and `MASTER_PLAN.md:104` (open unfulfilled Phase 0 task bullet "Update RELAYKIT_PRD_CONSOLIDATED…" — no `(completed …)` marker like L100/L101 have; superseded by the retirement; PM to mark `(superseded by retirement 2026-04-21)` or delete outright on next MASTER_PLAN touch); `PM_PROJECT_INSTRUCTIONS.md` (5 references at L212/L312/L360/L451/L589, Joel-owned file per Session 40/41 discipline); `DECISIONS.md:867` (historical reference inside an older decision's context, immutable). **Quality gates:** no code touched — `tsc`/`eslint`/`vitest` not required. Post-content-commit grep verification confirmed no live references to the retired files remain in active canonical docs; remaining matches live only in the archived files themselves, the archive directory, this REPO_INDEX change log (historical), the four flagged docs above, and `/src/**` (D-358 frozen). No new D-numbers. Two commits: content (`b7e93ad`, 6 files, 39/-50) + this close-out, both pending PM approval before push.
- **2026-04-21 (Session 41 — Phase 0 Group F; closes Phase 0):** `SRC_SUNSET.md` created at repo root (doc-only session). Plan executed from `~/.claude/plans/reality-correction-before-planning-logical-thacker.md` — approved with two clarifications (MASTER_PLAN §4 past-tense bullet applied in the content commit rather than close-out; sandbox-key row split into "Rebuild shipped in /api Session A pre-Phase 0" + "Prefix flip per D-349: Group E" to correctly attribute the actual rebuild). Covers all 23 `/src/app/api/*` routes plus `/src/components/**`, `/src/utils/supabase/**`, `/src/middleware.ts`, `/src/lib/twilio/**`, and AES-256-GCM subaccount-token encryption: 16 capability rows mapped to Phases 2/3/4/5, 1 completed Phase 0 row (sandbox key mgmt), 8 excluded/not-rebuilt rows (BYO Twilio waitlist, use-case endpoint, blueprint endpoints `build-spec`/`deliverable`/`message-plan`, `/dev/intake` dev-only route, unreachable `/auth` page, mostly-unused `hooks/`, all `lib/twilio/**`, subaccount encryption utility). Five **Open-F-N** mappings surfaced for target-phase resolution (Open-F-1 delivery-status webhook scope, Open-F-2 developer webhook CRUD placement, Open-F-3 Stripe checkout Phase 5 confirmation, Open-F-4 auth system Phase 5 confirmation, Open-F-5 compliance monitoring rebuild-vs-first-build scope). MASTER_PLAN.md §4 "What gets done" gained a Group F past-tense bullet after L100 (no version bump — subtask clarification). REPO_INDEX canonical docs table gained a row for `SRC_SUNSET.md`; active-plan-pointer Group F bullet flipped PENDING→COMPLETE. No code touched. No tsc/vitest/eslint gates (doc-only). No new D-numbers. Phase 0 closes.
- **2026-04-22 (Session 43 — MASTER_PLAN cross-ref cleanup + `/experiments/sinch/` scaffold; doc-only, between Phase 0 close and Phase 1 Experiment 1):** Plan executed from `~/.claude/plans/session-43-task-magical-llama.md`, PM-approved before execution. Three deliverables in two atomic commits. **Commit 1 — content (`42b151d`, 3 files, +60/-2):** MASTER_PLAN.md L33 narrative softened `"as of the CURRENT_STATE_AUDIT done this session"` → `"as of the Session 37 audit (archived Session 42)"`. MASTER_PLAN.md L104 open Phase 0 task bullet `"Update RELAYKIT_PRD_CONSOLIDATED to reflect current decision count and endpoint list"` deleted outright (superseded by the Session 42 retirement; supersession note would be archaeology noise since the retirement is captured in this change log). Together these clear the two Session 42-flagged cross-refs against MASTER_PLAN. No MASTER_PLAN version bump — subtask clarifications, v1.0 stays. `/experiments/sinch/experiments-log.md` created (59 lines) with proving-ground preamble (disposable code, fixture-driven prod, per MASTER_PLAN §5) + Experiment 1 block (Status: not yet run / Goal / Procedure referencing Sinch project `6bf3a837-d11d-486c-81db-fa907adc4dd4` + `send-one.mjs` fetch-only per D-02 + fixture target `/experiments/sinch/fixtures/exp-01-outbound.json` / Expected artifacts / Success criteria / empty Findings template with 7 bullets for Joel to fill in post-run). `/experiments/sinch/fixtures/.gitkeep` created to track the fixtures directory. **Commit 2 — close-out (this commit):** REPO_INDEX.md meta bumps (Last updated, Master plan last updated, Unpushed local commits), Subdirectories entry `/experiments/` flipped from "planned, not yet created" to `/experiments/sinch/` with scaffold description, active plan pointer flipped off Phase 0 onto Phase 1 with MASTER_PLAN §5-mirrored scope paragraph covering all five experiments, Phase 0 progress heading marked CLOSED (bullets preserved as historical record), this change log entry appended. CC_HANDOFF.md overwrite. **Cross-refs intentionally not touched (Session 42 flags still open):** `PM_PROJECT_INSTRUCTIONS.md` L212/L312/L360/L451/L589 (5 refs, Joel-owned per Session 40/41 discipline), `DECISIONS.md:867` (historical reference inside an older decision body, immutable), `docs/archive/PRD_04_TWILIO_SUBMISSION.md:3` (stale pointer inside an already-archived file). **Pre-flight git state:** HEAD `793a995` = origin/main at session start (Session 42's two commits pushed by Joel before Session 43); working tree had `M PM_PROJECT_INSTRUCTIONS.md` (Joel's standalone edit, not staged) + untracked `api/node_modules/` (intentional). **Verification after Commit 1:** `grep -n "CURRENT_STATE_AUDIT\|RELAYKIT_PRD_CONSOLIDATED" MASTER_PLAN.md` returned zero matches (clean). `git ls-files experiments/` confirmed both scaffold files tracked. **Quality gates:** no code touched — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates (apply to modified code directories). No new D-numbers. Two commits unpushed at Session 43 close — `42b151d` (content) + this close-out — both pending PM approval before push.
- **2026-04-23 (Session 44 — Phase 1 Experiment 1 + 1b capture; doc-only, first real Phase 1 finding landing in the log):** Plan executed in-session, PM-approved before run. Three deliverables in two atomic commits. **Commit 1 — content (`58e95a6`, 3 files, +97/-9):** Two new JSON fixtures under `/experiments/sinch/fixtures/` (`exp-01-outbound.json` 33 lines — successful XMS API send at 17:24:48Z, Bearer-token auth, `us.sms.api.sinch.com/xms/v1/{servicePlanId}/batches`, HTTP 201 in 240ms, full Sinch batch response with `id` ULID `01KPXNYXGCX1GYDBJ9HF0Y9CCM`, `phone_received: false`; `exp-01b-delivery-report-rejected.json` 17 lines — `delivery_report:full` request at 17:31:40Z returning HTTP 403 in 129ms with `{ code: "missing_callback_url", text: "Requesting delivery report without any callback URL." }`). Both fixtures use `{servicePlanId}` + `{SINCH_API_TOKEN}` placeholders — no live credentials committed. `experiments-log.md` Findings replaced from 7-bullet empty template to 9-bullet captured-result block (phone `+12013619609` at $1 setup + $1/month, legacy XMS Bearer token not OAuth2, endpoint + region, request/response shapes, 201 status, 240ms latency, phone-did-not-receive, error-shape cross-ref to 1b) + 5-item Implications-for-Phase-2-Session-B (auth is legacy XMS not OAuth2; silent carrier drops are baseline failure mode for unregistered traffic; ID is ULID 26-char not UUID; Sinch "Try the service" dashboard UI is broken; Open-F-1 delivery-status webhook scope gains real urgency). New Experiment 1b H2 section added (Status + Goal + 4-item Findings + `COMPLETE` footer). New Experiment 2 stub H2 section appended (inbound webhook + delivery report callback shape; Status: not yet run; Goal; Procedure stubbed `_To be drafted before running._`; Status: NOT STARTED). **Commit 2 — close-out (this commit):** REPO_INDEX meta bumps (Last updated, Unpushed local commits) + Subdirectories `/experiments/sinch/` entry updated to note 2 captured fixtures + this change-log entry + CC_HANDOFF.md overwrite + BACKLOG.md gains a new entry under Infrastructure & Operations (pre-Phase-2 DECISIONS.md audit: review D-84 through D-362 for internal consistency, identify superseded/conflicting decisions, propose consolidation; estimated 3–4 hours CC + 1–2 hours PM↔Joel; run before Phase 2 Session B kickoff). **Pre-flight git state:** HEAD `bd5f425` == `origin/main` at session start (Session 43's `42b151d` + `7393029` pushed by Joel between sessions along with Joel's standalone PM-instructions commit `bd5f425`). Clean working tree except untracked `api/node_modules/`. **Verification after Commit 1:** `git ls-files experiments/sinch/fixtures/` returned three files (`.gitkeep` + both new fixtures); `git show --stat 58e95a6` confirmed 3 files / +97 / -9. Markdown visual scan of `experiments-log.md` confirmed three H2 experiment blocks render correctly with `---` dividers and consistent status footers. **Quality gates:** no code touched — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates (apply to modified code directories). **Phase 1 progress:** Experiment 1 + 1b COMPLETE; Experiment 2 stubbed (procedure drafting is next PM task); Experiments 3, 4, 5 not started. **Phase 2 Session B outlook:** still GATED on Phase 1 but now has real success-path + error-path fixtures plus clear direction on auth (legacy XMS not OAuth2), ID format (ULID), and failure-detection strategy (delivery-report callbacks required — cannot detect carrier blocking from send-path response). Full unblock depends on Experiment 2 closing the callback-payload-shape gap (Open-F-1 converges here). No new D-numbers. Two commits unpushed at Session 44 close — `58e95a6` (content) + this close-out — both pending PM approval before push.
- **2026-04-23 (Session 45 — Experiment 2 stub replaced with 2a + 2b split; doc-only, procedure drafting only — no experiment runs this session):** Plan executed in-session, PM-approved before run. Two deliverables in two atomic commits. **Commit 1 — content (`b9f7200`, 1 file, +108/-3):** `experiments/sinch/experiments-log.md` — Session 44's "Experiment 2 — Inbound webhook + delivery report callback shape" stub (9 lines, Status: NOT STARTED, Procedure `_To be drafted before running._`) replaced with two new H2 blocks. **Experiment 2a — Delivery-report callback shape** (runnable): full 6-step procedure — build webhook receiver at `/experiments/sinch/webhook-receiver/` (Cloudflare Worker: `src/index.js` logs the incoming POST as JSON + returns 200, `wrangler.toml` with `compatibility_date = "2026-04-01"`) + `wrangler deploy`; configure Service Plan callback URL in Sinch dashboard (SMS → Service APIs → REST configuration, same panel as the XMS Bearer token from Experiment 1); `wrangler tail` + send with `delivery_report: "full"` to replicate Experiment 1 minus the 1b 403 rejection; wait and observe (two informative outcomes: callback arrives within ~60s → capture shape; OR no callback within 10min → implication = MNO doesn't notify Sinch either, Phase 2 needs timeout-based "presumed failed" heuristic alongside callback-driven state); write fixture to `/experiments/sinch/fixtures/exp-02a-delivery-report.json` with placeholders for all credentials; fill in 7-bullet Findings template. **Experiment 2b — Inbound MO message shape** (BLOCKED on Experiments 3 + 4): Status explicitly `BLOCKED — requires a delivery-capable sender. Unregistered 10DLC traffic from Experiment 1 does not reach the recipient phone, so there is no message to reply to and no inbound MO to capture.` Procedure stubbed `_To be drafted after Experiments 3 + 4 land and a registered sender exists. Reuses the webhook receiver from 2a._` **Rationale for the split:** Experiment 1's silent-drop finding means the delivery-report half (runnable now against unregistered traffic — either callback arrives or non-arrival is itself the data) is decoupled from the inbound-MO half (un-runnable until a deliverable message exists post-3+4). 2a carries higher immediate value for Phase 2 Session B — it's the failure-detection contract. Webhook receiver code is retained per the One Source Rule rather than thrown away (becomes a real Phase 2/4 prototype, lives at `/experiments/sinch/webhook-receiver/` as a staging ground). **Commit 2 — close-out (this commit):** REPO_INDEX meta bumps (Last updated, Unpushed local commits) + Subdirectories `/experiments/sinch/` entry rewritten to reflect the 2a/2b split (webhook-receiver directory noted as not-yet-created, fixtures directory state unchanged until 2a runs) + this change-log entry appended chronologically after Session 44 + CC_HANDOFF.md overwrite. **Pre-flight git state:** HEAD `7496c26` == `origin/main` at session start (Session 44's two commits pushed by Joel between sessions after PM approval). Clean working tree except untracked `api/node_modules/`. **Verification after Commit 1:** `grep -nE "^## Experiment\|^### Status:" experiments/sinch/experiments-log.md` returned four experiment blocks (1 / 1b / 2a / 2b) with expected status footers (1: COMPLETE, 1b: COMPLETE, 2a: NOT STARTED, 2b: BLOCKED on Experiments 3 + 4); `git show --stat b9f7200` confirmed 1 file / +108 / -3. **Quality gates:** no code touched — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates (apply to modified code directories). **Phase 1 progress:** 1 + 1b COMPLETE; 2a has full procedure and is ready for Joel-driven run; 2b BLOCKED on 3 + 4; 3, 4, 5 not started (procedures not yet drafted). **Phase 2 Session B outlook:** unchanged from Session 44 — still GATED on Phase 1, full unblock still depends on 2a closing the callback-payload-shape gap (2a's "either outcome is informative" design ensures Session B unblocks either way: callback shape captured, or silent-drop timeout heuristic required). No new D-numbers. Two commits unpushed at Session 45 close — `b9f7200` (content) + this close-out — both pending PM approval before push.
- **2026-04-24 (Session 47 — pre-Phase-2 DECISIONS audit, BACKLOG-promoted; doc-only, audit-only — no DECISIONS edits):** Plan approved in plan mode at `~/.claude/plans/decisions-audit-produce-a-giggly-umbrella.md`. Two deliverables in two atomic commits. **Commit 1 — content (`071f9b8`, 1 file created, 148 lines):** `audits/DECISIONS_AUDIT_2026-04-24.md` — first entry under new `audits/` directory. Read-only findings report covering D-01 through D-362 against MASTER_PLAN v1.1 and VOICE_AND_PRODUCT_PRINCIPLES_v2.0. Five categories per the standard audit structure: **§A direct contradictions (1):** D-89 → D-15 / D-37 propagation chain leaves a contradicted picture against D-294 even though D-89 itself carries the supersession mark. **§B superseded-without-notice (8):** pricing chain D-193/D-196/D-216/D-314 → D-320 (B-1); D-105 stale $199 figure post-D-320 (B-2); D-16 → D-89 index-only mark (B-3); D-17 → D-215 index-only mark (B-4); D-19 reframing by D-293 missing inline note (B-5); D-37 → D-294 index-only mark (B-6); D-42 → D-90 index-only mark (B-7); D-237/D-243/D-244 cluster orphaned by D-293 without inline notes (B-8). **§C orphaned (5 grouped findings, ~17 D-numbers):** Twilio-era /src cluster D-02/D-26/D-199/D-232 retired by D-358 (C-1); spec-file delivery model D-257/D-258/D-259/D-260/D-261/D-286 superseded by SDK D-266/D-279 (C-2); custom-message UI cluster D-64/D-72/D-79 orphaned by D-280 (C-3); D-162 download-flow noted as already inline-annotated (C-4, no action); plan-builder vestiges D-47/D-85 flagged for PM confirmation (C-5). **§D voice violations (2):** D-105 prescribes "10DLC" in marketing-page money-back guarantee copy (D-1); D-157 uses "leverage" in user-facing AI-command shortlist help text (D-2). **§E ambiguity (2):** D-101 "opt-in disclosure must enumerate implemented message types" — SDK output vs developer maintenance unclear (E-1); D-239 "prompt customer to enable SMS alerts after phone verification" — synchronous vs opportunistic unclear (E-2). **Appendix:** D-72 near-duplicate of D-64; D-244 says "supersedes D-233" but D-233 may need reciprocal note + D-244 itself is now orphaned per §B-8; D-241 → D-211 spot-check pending; D-358 doesn't enumerate every Twilio-era retirement so §C-1 cluster may be incomplete. **Patterns observed:** Three clusters dominate — (1) silent supersession via index-only annotation (the largest pattern, accounts for §B in entirety + significant §C overlap); (2) Twilio→Sinch + /src sunset pivot (D-358 + D-215); (3) runtime→authoring-time enforcement pivot (D-293). No high-stakes active contradictions where two competing approaches are both being built — supersession-recording discipline is good; inline-propagation discipline is not. **Commit 2 — close-out (`2e2207c`, since amended by fix-up commit below):** REPO_INDEX meta bumps (Last updated, Decision count, Unpushed local commits — initially overstated as 4) + canonical-docs table gains row for `audits/DECISIONS_AUDIT_2026-04-24.md` + new `### /audits` Subdirectories entry establishing the dated-report convention + this change-log entry appended chronologically after Session 46 + CC_HANDOFF.md overwrite.

**Commit 3 — fix-up close-out:** Corrects the unpushed-commit count from 4 to 2 in REPO_INDEX Meta block + CC_HANDOFF + this change-log entry. Root cause: Session 47's initial close-out inherited Session 46 CC_HANDOFF's "two unpushed pending PM approval" language without re-verifying against the remote. In reality, Joel/PM had already pushed Session 46's two commits to `origin/main` between sessions (HEAD == `950e824`), same pattern as Sessions 44→45 and 45→46. Session 47 should have done the standard `HEAD == origin/main` reality-check at session start; that step was skipped. The fix-up adds explicit corrective text in REPO_INDEX Meta and an explicit "Session-start reality check (correction)" paragraph in CC_HANDOFF, plus a "Lesson for next session" reminder that the reality-check is non-optional. **Out of scope (explicit, do-NOT items per plan):** modifying DECISIONS.md or DECISIONS_ARCHIVE.md; adding/removing/renumbering D-entries; rewriting any decision text; pushing to remote; touching any code under `/api`, `/sdk`, `/prototype`, `/src`; updating MASTER_PLAN.md, MESSAGE_PIPELINE_SPEC.md, PROTOTYPE_SPEC.md or any spec; running tsc/eslint/vitest. **Pre-flight git state (corrected):** HEAD `950e824` (Session 46 close-out) at session start; `origin/main` was ALSO at `950e824` (Joel/PM had already pushed Session 46's two commits between sessions, same pattern as Sessions 44→45 and 45→46). The standard `HEAD == origin/main` reality-check at session start was skipped, leading to the incorrect "ahead by 2" claim in the initial Session 47 close-out — corrected by the fix-up commit. Working tree carried intentional unstaged edits to `CLAUDE.md`, `DECISIONS.md`, `PM_PROJECT_INSTRUCTIONS.md` — coordinated PM-side rewrite formalizing DECISIONS ledger stewardship (CLAUDE.md adds pre-flight ledger scan, inline supersession enforcement with six gate tests, retirement sweep at phase boundaries; DECISIONS.md adds canonical entry template + six gate tests + supersession rule pointer; PM_PROJECT_INSTRUCTIONS.md +112/-43 lines of corresponding PM-side rules). Per Session 40/41 PM-edit-discipline these were left untouched and unstaged — they belong to PM. Untracked `api/node_modules/` (intentional). **In-session verification before Commit 1:** spot-checked 9 archive D-numbers for missing inline supersession annotations (D-16, D-17, D-19, D-37, D-42, D-64, D-72, D-79, D-82 — all confirmed lacking inline marks despite index-only marks); spot-checked 4 active-file D-numbers (D-237, D-243, D-244, D-314 — all confirmed lacking inline marks); spot-checked 2 voice-violation D-numbers for exact quote text (D-105 "10DLC" + $199, D-157 "leverage"). Three Explore-subagent passes (one per category cluster) ran in parallel during plan-mode research. **Verification after Commit 1:** `git status --short` clean except staged audit file (CLAUDE.md / DECISIONS.md / PM_PROJECT_INSTRUCTIONS.md drift untouched, intentional); `git diff --cached --stat` confirmed 1 file / +148 / -0; `wc -l audits/DECISIONS_AUDIT_2026-04-24.md` returned 148; `grep -c "^### " audits/DECISIONS_AUDIT_2026-04-24.md` returned 18 (1 A + 8 B + 5 C + 2 D + 2 E = 18 individual findings). **Quality gates:** no code touched — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates. **Triage handoff to PM + Joel:** the audit is findings-only by design; CC has executed zero DECISIONS or DECISIONS_ARCHIVE edits. Each finding's "Proposed resolution" is a recommendation for PM review. The new CLAUDE.md "Retirement sweep" rule (added in working-tree drift) explicitly mandates this audit-as-findings-only pattern going forward at phase boundaries; this Session 47 audit is the first concrete instance of that pattern (pre-Phase-2 timing per BACKLOG promotion, not a phase-boundary close). Any A/B/C/D/E action requires explicit PM approval before CC touches DECISIONS.md or DECISIONS_ARCHIVE.md. No new D-numbers. Two commits unpushed at Session 47 close (three with the fix-up): Session 47's `071f9b8` (audit) + `2e2207c` (initial close-out) + the fix-up close-out. All pending PM approval before push. Session 46's two commits (`1358f99` + `950e824`) are already on `origin/main`.
- **2026-04-23 (Session 46 — MASTER_PLAN.md v1.0 → v1.1 Phase 2 ↔ Phase 4 scope amendment; doc-only, MASTER_PLAN-only amendment):** Plan approved in plan mode at `~/.claude/plans/decisions-check-confirm-you-ve-mossy-hopcroft.md`. Two deliverables in two atomic commits. **Commit 1 — content (`1358f99`, 1 file, +26/-18):** `MASTER_PLAN.md` — four edits. **Edit 1:** L3 version header `### Version 1.0 — April 20, 2026` → `### Version 1.1 — April 23, 2026`. **Edit 2:** §5 Phase 1 item 2 bullet — old "Receive an inbound SMS (reply)" replaced with "Configure webhook receiver, capture callback payloads" describing the 2a/2b split from Session 45 (2a runnable delivery-report capture; 2b BLOCKED on Experiments 3 + 4 since unregistered traffic can't be replied to). **Edit 3:** §6 Phase 2 full-section replacement — heading renamed to "Sinch Outbound Delivery + Delivery-Report Callbacks"; gains a new paragraph on Experiment 1's silent-drop finding driving why callbacks belong in Phase 2 (Sinch's 201 is not evidence of delivery, unregistered 10DLC drops silently at the carrier, `/api` cannot truthfully report delivery without the callback loop, callback infrastructure originates as the Phase 1 Experiment 2a Cloudflare Worker and Phase 2 ports it into `/api` as a real route); gains a new paragraph on `messages.status` enum-semantics revision landing at Phase 2 Session B kickoff as a new D-number (settles whether `'sent'` means "submitted to carrier" or "delivered per callback" and introduces whatever intermediate state is required); "What gets done" list gains 4 new bullets (build delivery-report callback receiver as port of Experiment 2a Worker, implement Sinch webhook signature verification covering both delivery reports in Phase 2 and MO payloads in Phase 4 as one verification layer / two payload types, configure Sinch Service Plan callback URL to point at deployed `/api`, implement status-transition flow from submission-time state to terminal state on callback plus timeout-based "presumed failed" fallback for silent-drop case) plus one bullet on status-enum revision per kickoff decision plus expanded tests bullet covering both send-path and callback-path fixtures plus expanded verify bullet covering end-to-end delivery-report loop; "What does not get done" explicitly routes MO payloads to Phase 4 with one-paragraph rationale ("Phase 2 builds the webhook receiver infrastructure and handles delivery-report payloads only; Phase 4 extends the same receiver to dispatch MO payloads into the inbound-handler logic"); demo moment rewritten for callback-driven `'sent'`/`'failed'` status transitions; output expanded to note accurate delivery outcomes + webhook receiver infrastructure production-grade and ready for Phase 4 to extend. **Edit 4:** §8 Phase 4 full-section replacement — heading narrowed to "Inbound Message Handling (MO Replies)"; opening paragraph updated to cite Experiment 2b (blocked on Experiments 3 + 4 landing a delivery-capable sender) as the fixture source; gains a new paragraph explicitly crediting the webhook receiver endpoint itself and signature verification to Phase 2 ("The receiver endpoint is not new work in Phase 4; the MO-specific logic is"); "What gets done" list narrowed to MO-specific work (MO payload type detection in the existing receiver, registration lookup, consent ledger STOP/START update, developer webhook forwarding, inbound storage with schema extension in Phase 3, tests against Experiment 2b fixtures); "What does not get done" explicitly disclaims delivery-report callbacks / receiver building / signature verification as all Phase 2; demo moment unchanged; output narrowed from "Inbound works" to "Inbound MO handling works". **Rationale:** Phase 1 Experiment 1 (captured Session 44) proved Sinch's 201 is not evidence of delivery — unregistered 10DLC drops silently at the carrier. Phase 2's old acceptance criterion ("row written with `status: 'sent'`") was therefore not truthful without delivery-report callbacks; since the callback loop is inseparable from honest outbound delivery semantics, it belongs in Phase 2, not Phase 4. Phase 4 stays intact on its two-way-messaging-foundation premise but narrows to MO-specific work. **Commit 2 — close-out (this commit):** REPO_INDEX meta bumps (Last updated, Master plan last updated with four-edit enumeration, Unpushed local commits); canonical-docs table row for MASTER_PLAN.md updated (date 2026-04-21 → 2026-04-23; description expanded to note v1.1 and Session 46 scope amendment); this change-log entry appended chronologically after Session 45; CC_HANDOFF.md overwrite. **Out of scope (explicitly deferred):** MESSAGE_PIPELINE_SPEC.md Session B update (catches up at Phase 2 kickoff); `messages.status` enum-semantics D-number (lands at kickoff alongside spec catch-up); no code touches; no other canonical doc updates; no push (hard stop at PM flag step per user instruction). **Pre-flight git state:** HEAD `4372271` == `origin/main` at session start (Session 45's two commits pushed between sessions after PM approval). Clean working tree except untracked `api/node_modules/`. **Post-edit verification grep:** `Version 1.0` → 0 matches (was 1); `Sinch Outbound Delivery + Delivery-Report Callbacks` → 1 match (new §6 heading); `Phase 4 — Inbound Message Handling (MO Replies)` → 1 match (new §8 heading); `payload Phase 4 has to handle` → 0 matches (old §5 item 2 text gone); `^## 6\. Phase 2` → 1; `^## 8\. Phase 4` → 1; `^**Phase [0-9]* output:**` → 11 (unchanged — one per phase 0 through 10). `git show --stat 1358f99` confirmed 1 file / +26 / -18. **Quality gates:** no code touched — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates. No new D-numbers. Two commits unpushed at Session 46 close — `1358f99` (content) + this close-out — both pending PM approval before push.
- **2026-04-24 (Session 48 — DECISIONS audit cleanup execution; doc-only, three-commit cleanup under the new stewardship rules landed in `b3b1def`):** Plan approved in plan mode at `~/.claude/plans/decisions-audit-cleanup-session-robust-thacker.md`. Four atomic commits — three content, one close-out. **Commit 1 (`ab0730f`, 1 file, +21/-2):** DECISIONS.md — 19 inline annotations on active entries + 2 voice copy edits. B-1 pricing chain D-193/D-196/D-216/D-314 → D-320 (4 notes). B-2 D-105 amount-superseded-by-D-320. B-8 D-237/D-243/D-244 → D-293 (3 notes). C-1 active D-199 (two lines: orphaned-by-D-358 + also-resolved-by-D-215) + D-232 orphaned-by-D-358. C-2 D-257/D-258/D-259/D-260/D-261 → D-266+D-279 (5 notes) + D-286 partial-orphan (JSON-registry-path flagged for Phase 2 kickoff confirmation). C-5 active D-85 orphaned (plan-builder UI retired). Appendix D-233 → D-244. D-1 D-105 body: `10DLC registration` → `carrier registration`. D-2 D-157 body: `These commands leverage` → `These commands use`. **Commit 2 (`f174f94`, 1 file, +15/-1):** DECISIONS.md — append D-363 (Opt-in disclosure uses generic language, not enumerated message types; Supersedes D-101) + D-364 (SMS compliance alert feature deferred until evidence of need; Supersedes D-239 and D-201). Reciprocal inline supersession marks on D-101, D-201, and D-239 landed in the same commit per CLAUDE.md stewardship rule. **Commit 3 (`73eed86`, 1 file, +14/-0):** DECISIONS_ARCHIVE.md — 13 archive-style **⚠ annotations + 1 duplicate-note on D-72. A-1 D-15 → D-294. B-3 D-16 → D-89. B-4 D-17 → D-215. B-5 D-19 reframed-by-D-293. B-6 D-37 → D-294. B-7 D-42 → D-90. C-1 archive D-02 orphaned-by-D-358 + D-26 two-line (orphaned-by-D-358 + also out of scope per MASTER_PLAN §16 for BYO Twilio/Sinch). C-3 D-64/D-72/D-79 orphaned-by-D-280. C-5 archive D-47 orphaned. Appendix D-72 near-duplicate-of-D-64 note. **Audit resolution status (18/18):** 16 findings executed (A-1 propagated across D-15/D-16/D-37/D-89 chain; B-1 through B-8 all annotated; C-1/C-2/C-3/C-5 all annotated; D-1/D-2 voice rewrites; E-1 D-363 + E-2 D-364 plus reciprocals on D-101/D-201/D-239). 1 no-action (C-4 — D-162 already inline-annotated). 1 appendix item (D-241→D-211) surfaced as unsupported by decision texts — D-211 is about sandbox API key Regenerate, D-241 is about compliance copy touchpoints, zero topical overlap, no annotation added; deferred to PM for appendix-wording review. **Pre-existing drift surfaced, not fixed:** D-101 title/body run-together with no newline and no `Date:` — predates the audit, out of scope this session. **Pre-flight git state:** HEAD == `b3b1def` == `origin/main` at session start (Joel/PM pushed Session 47's stack + `b3b1def` between sessions; `b3b1def` commits the working-tree drift formalizing DECISIONS ledger stewardship that persisted across Sessions 40–47). Clean working tree except untracked `api/node_modules/`. Session-start reality check performed per Session 47 fix-up lesson. **Verification suite after Commit 3:** `grep -cE "^\*\*D-[0-9]+" DECISIONS.md` returned 279 (277 + D-363 + D-364); `grep -cE "^\*\*D-[0-9]+" DECISIONS_ARCHIVE.md` returned 83 (unchanged); every cited D-number (89, 90, 215, 244, 266, 279, 280, 293, 294, 320, 332, 358, 363, 364) resolved to a header line in one of the two files; `grep -c "10DLC registration is not approved" DECISIONS.md` returned 0 (was 1 pre-session); `grep -c "carrier registration is not approved" DECISIONS.md` returned 1; `grep -c "These commands leverage" DECISIONS.md` returned 0 (was 1); `grep -c "These commands use the AI tool" DECISIONS.md` returned 1; `git log --oneline b3b1def..HEAD` showed 3 content commits touching only DECISIONS.md + DECISIONS_ARCHIVE.md. **Quality gates:** doc-only — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates. Four commits unpushed at Session 48 close — `ab0730f` + `f174f94` + `73eed86` + this close-out — all pending PM approval before push. **Out of scope (explicit, do-NOT items per plan):** modifying any decision not enumerated in the triage; archive moves or renumbering; updating the archive-item index table at DECISIONS.md L57–L136 (inline-only remediation to stay scoped; index-summary harmonization is a separate ticket if PM wants it); touching any code under `/api`, `/sdk`, `/prototype`, `/src`; modifying MASTER_PLAN.md, PROTOTYPE_SPEC.md, or any spec; pushing to remote.
- **2026-04-24 (Session 49 — Experiment 2a webhook-receiver scaffold; prototype-phase throwaway Worker code per MASTER_PLAN §5, retained under the One Source Rule):** Plan approved in plan mode at `~/.claude/plans/task-scaffold-the-cloudflare-atomic-prism.md`. Two atomic commits — one scaffold, one close-out. **Commit 1 (`3d99a69`, 2 files created, 21 insertions):** `/experiments/sinch/webhook-receiver/src/index.js` (18 lines) + `/experiments/sinch/webhook-receiver/wrangler.toml` (3 lines). Both files reproduced verbatim from `experiments/sinch/experiments-log.md §Experiment 2a Procedure` step 1 — not new authoring. `src/index.js`: minimal Cloudflare Worker `export default { async fetch(request) { ... } }` that captures `{ timestamp, method, path, headers, body }` for each inbound request via `Object.fromEntries(request.headers.entries())` + `safeBody` helper (tries `JSON.parse`, falls back to raw text), logs the capture as formatted JSON, returns empty `Response('', { status: 200 })`. `wrangler.toml`: three lines — `name = "sinch-webhook-receiver"`, `main = "src/index.js"`, `compatibility_date = "2026-04-01"`. **Commit 2 — close-out (this commit):** REPO_INDEX meta bumps (Last updated → Session 49 summary, Unpushed local commits → 2 with commit hashes, Decision count unchanged at D-364 with Session-49-no-new-D-numbers note), Subdirectories `/experiments/sinch/` entry rewritten — `Code in \`webhook-receiver/\` (not yet created — will land when Joel runs 2a)` replaced with scaffolded-Session-49 phrasing (src/index.js + wrangler.toml listed, `wrangler deploy` + Sinch Service Plan callback URL config noted as awaiting Joel), One Source Rule clause + Phase 2/4 port note preserved verbatim; this change-log entry appended chronologically after Session 48; CC_HANDOFF.md overwritten with Session 49 handoff. **Out of scope (explicit, do-NOT items per plan and per Joel's task framing):** running `wrangler deploy` (Joel's step); modifying `experiments/sinch/experiments-log.md` (Findings capture is a later session after the send-and-observe run); adding README / package.json / .gitignore / tests / lint config / any other scaffolding (prototype-phase minimal per MASTER_PLAN §5, production hardening lands at Phase 2/4 port time); touching anything under `/experiments/sinch/` beyond the scaffold directory; modifying any canonical doc beyond REPO_INDEX.md + CC_HANDOFF.md at close-out; pushing to remote. **Pre-flight git state:** HEAD == `90a960c` == `origin/main` at session start (all four Session 48 commits plus the Session 48 fix-up commit `90a960c` pushed in-session at Session 48 close — see Session 48 entry above). Clean working tree except untracked `api/node_modules/`. Session-start reality check performed per Session 47 fix-up lesson. **In-session verification:** `ls -la experiments/sinch/` before write confirmed `webhook-receiver/` did not yet exist (only `experiments-log.md` + `fixtures/`). Post-Write: `wc -l experiments/sinch/webhook-receiver/src/index.js` → `18`; `wc -l experiments/sinch/webhook-receiver/wrangler.toml` → `3`; `grep -c "export default" experiments/sinch/webhook-receiver/src/index.js` → `1`; `grep -c "sinch-webhook-receiver" experiments/sinch/webhook-receiver/wrangler.toml` → `1`. Post-Commit-1: `git log --oneline 90a960c..HEAD` → 1 line (the scaffold commit); `git show --stat 3d99a69` → 2 files / +21 / -0. **Code-fidelity flags surfaced in plan, recommendation verbatim-adherence:** (1) URL search/query is not captured (`new URL(request.url).pathname` only) — correct for Sinch delivery-report callbacks since payload travels in POST body, `wrangler tail` still shows full URL for debugging; (2) `safeBody` swallows parse errors, returns raw text — clean fallback; (3) no explicit error handling on `req.text()` — fail-loud is desired for an experiment; (4) no CORS / auth / signature verification — correct for capture-only experiment, signature verification belongs in Phase 2 port per MASTER_PLAN §6; (5) response body empty with status 200 — satisfies Sinch's 2xx-ack expectation. All five are reproduction-fidelity notes, not design decisions; no D-number warranted. **Quality gates:** no production code touched — `tsc --noEmit` / `eslint` / `vitest` not applicable to throwaway Worker JS per MASTER_PLAN §5 (and per CLAUDE.md close-out gates, which apply to modified code directories under `/api`, `/sdk`, `/prototype`). No new D-numbers. Three commits unpushed at Session 49 close — `3d99a69` (scaffold) + `58ed165` (initial close-out) + this fix-up — all pending PM approval before push, then Joel deploys the Worker and fires the send. **Commit 3 (fix-up) rationale:** The initial close-out (`58ed165`) cited `e0ea648` as session-start HEAD, inheriting that reference from the Session 48 CC_HANDOFF header. `git log --oneline -5 origin/main` post-close revealed `90a960c` (the Session 48 handoff fix-up commit) actually sits above `e0ea648` on origin/main — so true session-start HEAD was `90a960c`, not `e0ea648`. Matches the established Session 47 + Session 48 fix-up pattern: correct the load-bearing field with a discrete fix-up commit rather than amend or leave stale. Fix-up touches REPO_INDEX.md (Meta Unpushed-commits line + this change-log entry's Pre-flight + Post-Commit-1 verification) and CC_HANDOFF.md (header + Commits This Session + Quality Checks + Gotcha 1 + Gotcha 6 + end note + new Commit 3 section).
- **2026-04-24 (Session 50 — Experiment 2a Findings capture + fixture write; doc-only, first real Phase 1 finding landing since Session 44):** Two atomic commits — one content, one close-out. **Commit 1 (`e811523`, 2 files, +89 / -9):** `experiments/sinch/experiments-log.md §Experiment 2a Findings` replaced from the 7-bullet empty template with 8 populated bullets covering callback-URL-configured / send-response / callback-received / callback-delay / callback-payload-shape / callback-event-types / transport-signals-including-no-signature-header / silent-drop-implication-with-hypothesis-overturn. §Experiment 2a Status footer flipped `NOT STARTED` → `COMPLETE — captured 2026-04-24`. `experiments/sinch/fixtures/exp-02a-delivery-report.json` created using the log step-5 nested schema (`api_request` / `api_response` / `api_timing_ms` / `callback_received` / `callback_request` / `callback_delay_ms` / `captured_at` / `notes`), different from exp-01/1b's flat schema because 2a's fixture captures both send-path + callback-path. `{servicePlanId}` + `{SINCH_API_TOKEN}` placeholders redact live credentials. `api_timing_ms` recorded as `null` (curl -i without timing flags) with explanation in top-level `notes`. **Key captured data:** send at `2026-04-24T20:38:57.257Z` returned 201 with batch ULID `01KQ0KF3Z9EM2JZZ468HVYZ9PD`; callback arrived at `2026-04-24T20:38:59.026Z`, delay 1769ms / 1.769s; canonical payload shape `{ batch_id, statuses: [{ code: 310, count: 1, recipients: ["12066013506"], status: "Failed" }], total_message_count: 1, type: "delivery_report_sms" }`; callback `user-agent: Apache-HttpClient/5.5.1 (Java/21.0.5)` with Cloudflare proxy headers (`cf-connecting-ip`, `cf-ipcountry`); **no signature / HMAC header on callback**. Phone did NOT receive the SMS — same silent carrier drop as Experiment 1, but the carrier surfaced that outcome to Sinch + to us via callback within ~2 seconds. **Hypothesis overturn (Session 45):** the Session 45 stub said "no callback within 10 minutes → MNO doesn't notify Sinch either → Phase 2 needs timeout-based presumed-failed heuristic alongside callback-driven state, not just callback-driven state." Session 50 data overturns this: callbacks DO arrive for carrier failures, and arrive fast. Phase 2 Session B's failure-detection strategy is **callback-primary** with timeout fallback as a safety net for transport failures (Worker down, Sinch bug) — not the other way around. **No D-number this session:** the overturn is a finding about Sinch's behavior, not a RelayKit decision. The `messages.status` enum revision reserved for Phase 2 Session B kickoff (per MASTER_PLAN §6) now has concrete input — `'sent'` = submitted-to-carrier at api-request time; terminal `'delivered'` / `'failed'` set from callback; at least one intermediate "submitted, awaiting callback" state required — but the enum-semantics D-number itself still lands at kickoff per Session 46 design, not this session. **Flag for Phase 2 Session B kickoff discussion (not a new D-number this session):** Sinch XMS delivery reports carry no signature / HMAC header. Standard signed-header verification is not viable. Alternatives: Sinch IP allowlist, mTLS, secret path segment in callback URL, or a Sinch dashboard feature not enabled by default. Successor question to the original Open-F-1 (delivery-status webhook scope, from `SRC_SUNSET.md`). **Commit 2 — close-out (this commit):** REPO_INDEX meta bumps (Last updated → Session 50 summary, Decision count unchanged at D-364 with Session 49 + 50 no-new-D-numbers note, Unpushed local commits → 2 with hashes); Subdirectories `/experiments/sinch/` entry rewritten — Session 49 scaffold language replaced with Session 50 COMPLETE language for 2a (fixture count 2 → 3; canonical payload shape + callback-delay + hypothesis-overturn noted inline), 2b still BLOCKED; Phase 2/4 port clause preserved; One Source Rule clause preserved. Build spec status MESSAGE_PIPELINE_SPEC Session B row — status column stays `GATED` (Experiments 3 + 4 + 5 still outstanding), description expanded to note 2a closure + hypothesis overturn + callback-primary failure-detection pattern. Active plan pointer Experiment 2 line flipped from Session 43's original "inbound reply webhook" seed to the 2a/2b split (2a COMPLETE with key captured-data bullets, 2b still BLOCKED on 3 + 4). This change-log entry appended chronologically after Session 49. CC_HANDOFF.md overwritten with Session 50 handoff. **Pre-flight git state:** HEAD == `3d397a6` == `origin/main` at session start (all three Session 49 commits `3d99a69` + `58ed165` + `3d397a6` pushed in-session by Joel via `git push origin main` at Session 49 close; `90a960c..3d397a6` already on remote). Session-start reality check performed per Session 47 fix-up lesson (`git rev-list --left-right --count HEAD...origin/main` returned `0 0` at session start). Working tree clean except untracked `api/node_modules/`. **In-session verification:** `python3 -m json.tool fixtures/exp-02a-delivery-report.json > /dev/null` → exit 0 (valid JSON). `grep -n "^### Status" experiments/sinch/experiments-log.md` → four status footers (1: COMPLETE 2026-04-23, 1b: COMPLETE 2026-04-23, 2a: COMPLETE 2026-04-24, 2b: BLOCKED on Experiments 3 + 4). Post-Commit-1: `git log --oneline 3d397a6..HEAD` → 1 line (`e811523 feat(experiments): capture Experiment 2a findings`); `git show --stat e811523` → 2 files / +89 / -9. **Pre-existing drift noted, not fixed this session:** Active plan pointer Experiment 1 line at L152 still says "not yet run" (stale since Session 44 capture); task scope was the Experiment 2 line only. Flagged in CC_HANDOFF for a future harmonization pass. **Quality gates:** no code touched — `tsc --noEmit` / `eslint` / `vitest` not required per CLAUDE.md close-out gates (apply to modified code directories under `/api`, `/sdk`, `/prototype`). **Phase 1 progress post-Session-50:** 1 + 1b + 2a COMPLETE; 2b BLOCKED on 3 + 4; 3, 4, 5 not started (procedures not yet drafted — PM-side work when PM hands over). **Phase 2 Session B outlook:** delivery-path axis now fully unblocked by captured shapes; remaining gate is registration + STOP/START/HELP evidence from Experiments 3 + 4 + 5. Session B kickoff remains the correct boundary for the enum-semantics D-number + MESSAGE_PIPELINE_SPEC spec catch-up + signature-verification design decision. No new D-numbers. Two commits unpushed at Session 50 close — `e811523` (content) + this close-out — both pending PM approval before push.
