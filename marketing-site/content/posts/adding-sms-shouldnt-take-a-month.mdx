---
title: "Adding SMS shouldn't take a month"
slug: adding-sms-shouldnt-take-a-month
date: "2026-05-15"
cluster: compliance-registration
lane: supply
status: published
description: "Why 10DLC registration drags on for weeks at most providers — and how it can take days instead."
---

## Placeholder content — replace before publishing

This post exists to exercise the blog rendering pipeline. Every element below
is here to verify styling, not to be read as real editorial copy. A content
task will replace this body with the real piece before launch.

The paragraph you're reading is plain body text. It should render in the
tertiary text token at a relaxed line height, inside a reading-width column of
roughly sixty-eight characters. Smart quotes matter here: "straight quotes"
should curl, and an apostrophe like the one in *shouldn't* should too.

### A third-level heading

Subsections use a third-level heading. Below it, a short paragraph with some
**bold emphasis** and some *italic emphasis* so both inline styles are visible.

You can also drop in `inline code` mid-sentence — it should get a small pill
background distinct from the dark code-block surface.

### Lists

An unordered list:

- Fast registration is the headline promise.
- Compliance should be invisible to the developer.
- Pricing is stated plainly on every surface.

And an ordered list:

1. Register the brand.
2. Register the campaign.
3. Send the first real message.

### A code block

```ts
import { RelayKit } from "relaykit";

const relay = new RelayKit({ apiKey: process.env.RELAYKIT_API_KEY });

await relay.messages.send({
  to: "+15555550123",
  body: "Your code is 481920.",
});
```

The block above should be syntax-highlighted by Shiki at build time, with no
client-side JavaScript involved.

### A blockquote

> Compliance work is the wall most indie founders hit. The goal is to make that
> wall something the product handles, not something the developer studies.

### A table

| Step       | Typical elsewhere | Target here |
| ---------- | ----------------- | ----------- |
| Brand      | 1–2 weeks         | A few days  |
| Campaign   | 1–3 weeks         | A few days  |
| First send | After both clear  | Same day    |

### A link

Read the [privacy policy](/privacy) for how data is handled, or see the
[10DLC overview on the Sinch site](https://www.sinch.com) for background.

---

That horizontal rule marks the end of the placeholder body. If you can see
every element above rendered with the correct tokens, the scaffold works.
